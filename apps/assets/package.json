{
	"name": "assets",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"deploy": "wrangler pages deploy build/client",
		"dev": "vite",
		"typecheck": "tsc",
		"typegen": "echo 'No typegen for assets'"
	},
	"devDependencies": {
		"vite": "^6.4.1"
	}
}
